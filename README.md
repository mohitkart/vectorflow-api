# Backend Service (Python)

This repository contains the backend service built using **Python**.

---

## 📦 Prerequisites

Make sure you have the following installed on your system:

- Python 3.9+
- Node.js (required to run npm scripts)
- npm

---

## ⚙️ Installation

Install all backend dependencies using the following command:

```bash
npm run install-req
```

Create the environment file by copying the example file:

```bash
cp env-example .env
```
Update the .env file with the required configuration values.


Start the backend server using:
```bash
npm start
```